const numbers = [1, 2, 3, 4];

const doubled = numbers.map(function(num) {
    return num * 2;
});

console.log(doubled);

// Hàm map tạo ra một mảng mới từ các giá trị trong mảng. Nhân đôi các phần tử trong mảng.
